{
    "name": "az-trainer",
    "private": true,
    "type": "module",
    "description": "Tooling for AZ Trainer's scripts: type checking and tests.",
    "scripts": {
        "test": "node --test \"tests/js/**/*.test.mjs\"",
        "typecheck": "tsc -p configs/jsconfig.json"
    },
    "devDependencies": {
        "typescript": "5.9.3"
    }
}
